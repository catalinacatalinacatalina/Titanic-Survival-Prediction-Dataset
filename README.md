# README

## Datasets
Se va a usar **dataset.csv**, pero el original es **original-dataset.csv**. Si ocurriera algún problema como que se modificara por error, mirad el valor original comparándolos: seleccionais los dos, click derecho, compare selected  (fijaros que los ids no son los mismos)

## Entorno
He creado el setup_env.bat para que lo ejecuteis y se os instale el entorno solo. Cuando se os ejecute, se os crearan archivos en la carpeta, metedlos en el gitignore: creais un archivo .gitignore y metéis lo siguiente

```
/.venv
/venv
/requirements.txt
```